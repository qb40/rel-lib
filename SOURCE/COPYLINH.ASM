MODEL MEDIUM,BASIC
.STACK 20H
.386
.CODE

;SUB RELCOPYLINEH(BYVAL DESTSEG,BYVAL SRCSEG,BYVAL YPOS)
;STACK
;DEST SEG       =8
;SRC  SEG       =6
;YPOS           =4
;QB RETURN SEG	=2
;QB RETURN OFF	=0
;ES:DI		=DEST SEG:DEST OFF
;DS:SI          =SRC SEG:SRC OFF

PUBLIC RELCOPYLINEH
RELCOPYLINEH PROC

MOV BX,BP
MOV DX,DS

MOV BP,SP
XOR DI,DI
MOV ES,[BP+8]
MOV DS,[BP+6]
MOV AX,[BP+4]
XCHG AH,AL
MOV DI,AX
SHR AX,2
ADD DI,AX
MOV SI,DI

MOV CX,80
REP MOVSD

MOV BP,BX
MOV DS,DX

RET 10

RELCOPYLINEH ENDP
END
